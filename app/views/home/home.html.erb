<% if user_signed_in? %>
  <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="delete">Log Out</a>
<% else %>
  <a class="nav-link" href="<%= new_user_session_path %>" data-method="delete">Sign in</a>
<% end %>