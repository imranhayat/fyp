<nav class="navbar navbar-expand bg-white border-bottom py-0">
  <div class="container-xl">
    <a class="navbar-brand text-primary py-0" href="/"><img src="/logo.png"/></a>
    <div>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <% if current_user&.role === "admin" %>
            <li class="nav-item">
              <a class="nav-link" href="/allusers">All Users</a>
            </li>
          <% else %>
            <li class="nav-item">
              <a class="nav-link" href="/users/edit">Update User</a>
            </li>
          <% end %>
          <li class="nav-item">
            <a class="nav-link"  href="<%= destroy_user_session_path %>" data-method="delete">Logout</a>
          </li>
        <% else %>
          <li class="nav-item">
          <a class="nav-link" href="<%= new_user_session_path %>">Sign In</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_registration_path %>">Sign Up</a>
          </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>